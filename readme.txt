I want to when user closes the tab on the browser it automatically trigger the stop_app.bat.
and also it should stop the the server because for now I have to stop the server by pressing the (ctrl + c) in the start_app.bat.


del "AUPE_RiskMonitor.bat" >nul 2>&1
del "Stop.bat" >nul 2>&1